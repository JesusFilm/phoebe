---
"phoebe-agent": patch
---

Recognise current `gh`'s `'<label>' not found` error so a missing `merged-to-feature` (or `processing`) label is created on first use instead of failing the landed-member write.
