# Write the requested documentation

You are working issue #{{ISSUE_NUMBER}}, a `docs-wanted` request.

1. Read the issue: `gh issue view {{ISSUE_NUMBER}}`.
2. Write or update the documentation it asks for, matching the style of the
   surrounding docs.
3. Run `{{READY_COMMAND}}` and fix anything it reports.
4. Commit your work with a clear message referencing #{{ISSUE_NUMBER}}.

Open a PR against `{{PR_BASE}}` only if you created commits.
